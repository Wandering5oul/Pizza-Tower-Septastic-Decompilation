if (other.state == UnknownEnum.Value_42)
{
    with (other)
        scr_pummel();
    
    with (baddieID)
    {
        state = UnknownEnum.Value_0;
        image_xscale *= -1;
    }
    
    instance_destroy();
}
