var j, m, i, b;

for (j = 0; j < array_length(menus); j++)
{
    m = menus[j];
    
    for (i = 0; i < array_length(m.options); i++)
    {
        b = m.options[i];
        
        if (b.type == UnknownEnum.Value_3)
        {
        }
    }
}
